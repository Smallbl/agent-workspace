
## 晚间记忆更新（21:14）

### Ollama 本地部署完成
- Ollama 版本：0.21.0（通过下载安装包安装，非 brew）
- Embedding 模型：`nomic-embed-text`（274MB，本地离线运行）
- 服务地址：http://127.0.0.1:11434
- VPN 环境下拉取成功

### OpenClaw 配置变更
- 新增 `models.providers.ollama`（baseUrl: http://127.0.0.1:11434）
- `agents.defaults.memorySearch.provider` 从 `none` 改为 `ollama`
- 效果：所有 Agent 的向量搜索从 `fts-only` 升级为 `hybrid`（向量 + 全文混合）
- 备份文件：`~/.openclaw/openclaw.json.bak.20260417190000`

### 做梦功能已开启
- `plugins.entries.memory-core.config.dreaming.enabled: true`
- 凌晨 3 点自动执行 light → REM → deep 三阶段 sweep
- 向量 API 已就位，Relevance 评分不再降级

### 待补充
- mxbai-embed-large 因网络问题未拉取，nomic-embed-text 暂时够用
- 如果未来需要更强的 embedding 能力，可考虑重新拉取 mxbai

### Gateway 重启丢消息问题处理
- 根因：config 变更触发 SIGTERM，立即中断正在处理的消息
- 症状：reply 丢失，用户看到「没反应」
- 解决方案：
  1. 创建安全重启脚本 `~/.scripts/safe-gateway-restart.sh`（10秒缓冲）
  2. 更新 AGENTS.md 和 TOOLS.md 添加操作规范
  3. 多个 config 变更统一一次重启
