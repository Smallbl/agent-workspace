# 核心配置

## 版本状态

- 当前运行版本：**v2026.5.18**（50a2481，05-17确认；建议升级到v2026.5.20）
- 升级路径：v2026.4.25 → v2026.5.12（跨越多个小版本）
- 备份路径：`~/.openclaw-backup-2026-04-26/`（83MB，升级前保留）
- 升级命令：`openclaw gateway stop` → `npm update -g openclaw` → 重启

## 目录结构

- 工作目录：`~/.openclaw/workspace`
- 记忆目录：`~/.openclaw/workspace/memory/`
- Git备份：已启用（`origin-workspace/main:config-backup` 分支）

## 关键配置文件

- `~/.openclaw/openclaw.json` — 主配置
- `~/.openclaw/cron/jobs.json` — 定时任务配置
- `~/.openclaw/agents/*/agent/` — 各 Agent 配置

## 目录结构

- 工作目录：`~/.openclaw/workspace`
- 记忆目录：`~/.openclaw/workspace/memory/`
- Git备份：已启用

## 关键配置文件

- `~/.openclaw/openclaw.json` — 主配置（agents.defaults.timeoutSeconds=600）
- `~/.openclaw/cron/jobs.json` — 定时任务配置
- `~/.openclaw/agents/*/agent/` — 各 Agent 配置

## 本地模型（Ollama）

- Ollama 版本：0.21.0（安装包方式安装，Mac Mini M4）
- 服务地址：http://127.0.0.1:11434
- Embedding 模型：nomic-embed-text（274MB，完全离线）
- 配置路径：`models.providers.ollama` + `agents.defaults.memorySearch.provider: "ollama"`
- 备份：`~/.openclaw/openclaw.json.bak.20260417190000`

## 做梦功能（Dreaming）

- 状态：已开启（`plugins.entries.memory-core.config.dreaming.enabled: true`）
- 执行时间：每天凌晨 3 点（`0 3 * * *`）
- 三阶段：light → REM → deep
- 向量 API：已就位（Ollama nomic-embed-text）
