【任务编号】TASK-20260402-001-v2
【任务标题】修复每日任务编辑保存的JS错误
【问题/需求描述】用户修改任务日期后点击保存，前端报错：TypeError: Cannot read properties of null (reading 'value')
【已定位信息】
1. 根因已找到：daily.html 第730行和第701行
   - `data.source = document.querySelector('#editForm input[name="source"]:checked').value;`
   - 当没有radio被选中时，querySelector返回null，访问.value报错
2. getFormData函数本身已经有null检查，不是getFormData的问题
3. 后端API已修复（curl测试返回200，数据持久化正常）
【需要执行内容】
修复第730行（编辑表单）和第701行（新增表单）：
- 添加null检查，防止querySelector返回null时崩溃
- 参考修复：`const checked = document.querySelector(...); data.source = checked ? checked.value : 'planned';`
【预期结果】
- 前端无JS错误
- 保存后刷新页面，数据保持不变
【超时设置】5分钟
